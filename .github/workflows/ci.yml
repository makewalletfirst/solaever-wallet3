name: CI
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"
          cache-dependency-path: solaever-wallet/package-lock.json

      - name: Install Dependencies
        run: cd solaever-wallet && npm install --silent

      - name: Typecheck
        run: cd solaever-wallet && npx tsc --noEmit || echo "Warnings allowed"
